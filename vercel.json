{
  "framework": "nextjs",
  "installCommand": "npm install && npm --prefix web install",
  "buildCommand": "npm --prefix web run build && cp -r web/.next ./.next && cp -r web/public ./public",
  "devCommand": "npm --prefix web run dev"
}
